@inject HttpClient Http
@inject NavigationManager Navigation
@using Elwin.GoGroceries.Client.Components.Forms
@using Elwin.GoGroceries.Client.Helpers;
@using Elwin.GoGroceries.Contracts;

@page "/categories/add"

<MudPaper Width="100%" Class="pa-4">
    <CategoryForm OnSave="Add"></CategoryForm>
</MudPaper>

@code {

    protected async void Add(CategoryDto dto)
    {
        var res = await Http.PostAsync("Categories", JsonSerializerHelper.Process(dto));

        if (res.IsSuccessStatusCode)
        {
            Navigation.NavigateTo($"/categories");
        }
    }
}
